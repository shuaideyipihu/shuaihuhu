# 2026-03-19

- The quant learning website is live on GitHub Pages at `https://shuaideyipihu.github.io/shuaihuhu/`.
- Current publishing model: GitHub Pages serves from the repository `docs/` folder. Public updates only appear when the latest website files are present under `docs/` and pushed to GitHub.
- A helper publish script now exists at `scripts/publish-quant-site.sh` to sync `quant-course/site/` into `docs/`, bump cache-busting query strings, and prepare a commit before push.
- Browser cache caused confusion during deployment. Cache-busting query strings were added to `docs/index.html` for `app.js` and `styles.css`.
- User wants website/product work to proceed in batches rather than step-by-step interruption: do several meaningful iterations, then report what changed.
- The user explicitly asked for these website fixes first, and they were implemented in site/docs source:
  - restore weeks 1–3 in the roadmap
  - expand lesson content substantially
  - make next-lesson navigation clickable
  - add a separate "show answer" button in exercises
  - allow exercise completion to be toggled on/off
- `skills/stock-watch/` was expanded beyond basic quotes: it now supports a default AI watchlist, near-real-time quote snapshots via Yahoo chart endpoints, trading-assistant labels, setup flags, risk flags, and simple alert-condition checks.
- The user wants future market/trading analysis to be concrete and data-driven first; vague commentary without current price/catalyst context is not acceptable.
